# Portfolio Phase 1 — Carbis + Moore County

## Preserve
Do not remove or overwrite any existing case study or portfolio feature.

## Add
1. `case-study-carbis-infrastructure.html`
2. `case-study-moore-county-discovery.html`
3. New sanitized image directories under `assets/img/`
4. Append the two blocks in `PHASE1_INDEX_PATCH.html` to the existing case-study grid.

## Important
The GitHub connector could read the repository but returned HTTP 403 when asked to create the working branch. No live repository files were changed. This package is intentionally staged for review/manual upload or a later write-capable connection.
